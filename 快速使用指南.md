# 金相图像ML分析平台 - 快速使用指南

> **版本**: v1.0  
> **更新日期**: 2026-06-19  
> **适用项目**: 基于机器学习的激光功率优化及JG-1铁基合金Q355钢组织性能协同调控研究

---

## 📋 目录

1. [系统概述](#系统概述)
2. [快速启动](#快速启动)
3. [功能说明](#功能说明)
4. [使用流程](#使用流程)
5. [输出文件说明](#输出文件说明)
6. [常见问题](#常见问题)
7. [更新日志](#更新日志)

---

## 系统概述

### 核心功能

| 模块 | 功能 | 技术栈 |
|------|------|--------|
| **图像分割** | 金相组织自动分割（熔覆层/析出相/缺陷） | OpenCV传统图像处理 |
| **定量分析** | 组织面积占比、晶粒尺寸、稀释率等 | Pandas数据统计 |
| **机器学习** | 回归预测（RFR/XGBoost/GBDT/KNN） | scikit-learn, XGBoost |
| **超参数优化** | Bayesian优化（Optuna TPE） | Optuna |
| **可解释性** | SHAP特征重要性分析 | SHAP |
| **多目标优化** | BFGS拟牛顿法Pareto前沿求解 | NumPy |
| **Web界面** | 交互式图表生成 | Streamlit |

### 文件结构

```
金相图片/
├── picture processing.py      # 主程序（图像处理+ML）
├── streamlit_app.py           # Web前端界面
├── 启动.bat                   # 一键启动脚本
├── 900W/                      # 900W功率组图像
├── 1200W/                     # 1200W功率组图像
├── 1500W/                     # 1500W功率组图像
├── 1800W/                     # 1800W功率组图像
└── analysis_output/           # 分析结果输出
    ├── 金相定量表征数据汇总.csv
    ├── segment_label_img/     # 分割结果图像
    ├── ml_results/            # ML评估报告
    ├── model_cache/           # 模型缓存
    └── *.png                  # 各种分析图表
```

---

## 快速启动

### 方法1：一键启动（推荐）

双击 `启动.bat` 文件即可：
- 自动创建虚拟环境
- 自动安装依赖
- 自动启动Web界面
- 浏览器自动打开

### 方法2：手动启动

```bash
# 1. 激活虚拟环境
.venv\Scripts\activate

# 2. 启动Streamlit
streamlit run streamlit_app.py
```

### 访问地址

- **本地URL**: http://localhost:8501
- **网络URL**: http://10.117.125.41:8501（局域网访问）

---

## 功能说明

### 1. 图像分割与定量分析

**输入**: TIFF格式金相图像（900W/1200W/1500W/1800W文件夹）

**输出**:
- 分割掩码图像（5类组织）
- 定量数据CSV（组织面积占比、晶粒尺寸等）
- Word实验报告

**组织分类**:
| 类别 | 颜色 | 说明 |
|------|------|------|
| 0 | 灰色 | 背景/基体 |
| 1 | 绿色 | 熔覆层组织 |
| 2 | 黄色 | 析出相/碳化物 |
| 3 | 红色 | 气孔缺陷 |
| 4 | 紫色 | 裂纹缺陷 |

### 2. 机器学习模型

**回归模型**:
- **RFR** (Random Forest Regressor)
- **XGBoost** (eXtreme Gradient Boosting)
- **GBDT** (Gradient Boosting Decision Tree)
- **KNN** (K-Nearest Neighbors)

**评估指标**:
- **R²** (决定系数，越接近1越好)
- **MSE** (均方误差，越小越好)
- **RMSE** (均方根误差，越小越好)
- **MAE** (平均绝对误差，越小越好)

### 3. Bayesian超参数优化

**算法**: Optuna TPE (Tree-structured Parzen Estimator)

**优化参数**:
- RFR: n_estimators, max_depth, min_samples_split, min_samples_leaf, max_features
- XGBoost: n_estimators, max_depth, learning_rate, subsample, colsample_bytree, reg_alpha, reg_lambda
- GBDT: n_estimators, max_depth, learning_rate, subsample, min_samples_leaf
- KNN: n_neighbors, weights, p

**默认设置**: 20 trials（可在代码中修改）

### 4. SHAP可解释性分析

**功能**:
- 全局特征重要性排序
- 局部特征贡献分析
- 可视化特征影响

**支持模型**: XGBoost, RFR

### 5. 多目标优化

**方法**: BFGS拟牛顿法

**优化目标**:
- 最小化目标1（如：气孔率）
- 最小化目标2（如：裂纹率）

**输出**: Pareto前沿解集

### 6. Web界面图表

| 图表名称 | 说明 | 格式 |
|----------|------|------|
| Pearson相关系数矩阵 | 特征相关性热力图 | 热力图+CSV |
| 模型误差指标对比 | R²/MSE/RMSE/MAE对比 | 2×2折线图 |
| ML模型预测结果 | 实际值vs预测值 | 2×2散点图 |
| Bayesian优化性能对比 | 优化前后性能对比 | 2×2雷达图 |
| SHAP特征重要性 | 特征贡献排名 | 条形图 |
| Pareto前沿 | 多目标优化结果 | 散点图 |
| 优化历史 | BFGS收敛曲线 | 折线图 |
| 数据预览 | 原始数据表格 | 表格 |

---

## 使用流程

### 流程图

```
┌─────────────────────────────────────────────────────────┐
│  步骤1: 图像分割与定量分析                                │
│  运行: python "picture processing.py"                   │
│  输出: analysis_output/金相定量表征数据汇总.csv          │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  步骤2: 启动Web界面                                      │
│  运行: 双击 启动.bat 或 streamlit run streamlit_app.py   │
│  访问: http://localhost:8501                             │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  步骤3: 训练/加载ML模型                                   │
│  点击: 侧边栏 "🚀 训练/加载模型" 按钮                     │
│  说明: 首次训练约2-3分钟，后续秒级加载（缓存）           │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  步骤4: 生成图表                                          │
│  选择: 侧边栏图表类型下拉框                               │
│  点击: "📈 生成图表" 按钮                                 │
│  下载: 图表下方下载按钮                                   │
└─────────────────────────────────────────────────────────┘
```

### 详细步骤

#### 步骤1: 图像分割与定量分析

**前提条件**:
- 在项目根目录下创建4个文件夹：`900W/`, `1200W/`, `1500W/`, `1800W/`
- 将对应功率的TIFF图像放入对应文件夹

**运行命令**:
```bash
python "picture processing.py"
```

**输出结果**:
- `analysis_output/金相定量表征数据汇总.csv` - 定量数据表
- `analysis_output/segment_label_img/` - 分割结果图像
- `激光熔覆金相定量分析实验报告.docx` - Word报告

#### 步骤2: 启动Web界面

**方法1**: 双击 `启动.bat`

**方法2**: 手动启动
```bash
.venv\Scripts\activate
streamlit run streamlit_app.py
```

**访问**: 浏览器打开 http://localhost:8501

#### 步骤3: 训练/加载ML模型

**操作**:
1. 在侧边栏找到 "2. ML模型训练"
2. 点击 "🚀 训练/加载模型" 按钮
3. 等待进度条完成（首次约2-3分钟）

**说明**:
- 首次运行会训练所有模型并缓存
- 后续运行会自动从缓存加载（秒级）
- 数据变更后会自动重新训练

**停止训练**:
- 点击 "⏹ 停止训练" 按钮可随时中断

#### 步骤4: 生成图表

**操作**:
1. 在侧边栏 "3. 图表生成" 下拉框选择图表类型
2. 点击 "📈 生成图表" 按钮
3. 查看生成的图表
4. 点击 "下载图表" 保存PNG/CSV

---

## 输出文件说明

### 主要输出文件

| 文件路径 | 说明 | 格式 |
|----------|------|------|
| `analysis_output/金相定量表征数据汇总.csv` | 定量表征数据 | CSV |
| `analysis_output/金相定量表征数据汇总.xlsx` | 定量表征数据（Excel） | XLSX |
| `analysis_output/pearson_correlation_matrix.csv` | Pearson相关系数矩阵 | CSV |
| `analysis_output/pearson_correlation_matrix.png` | 相关系数热力图 | PNG |
| `analysis_output/model_error_metrics.png` | 模型误差指标对比 | PNG |
| `analysis_output/bayesian_optimization_performance.png` | Bayesian优化性能对比 | PNG |
| `analysis_output/model_prediction_results.png` | ML模型预测结果 | PNG |
| `analysis_output/shap_importance.png` | SHAP特征重要性 | PNG |
| `analysis_output/pareto_front.png` | Pareto前沿 | PNG |
| `analysis_output/optimization_history.png` | 优化历史 | PNG |
| `analysis_output/ml_results/模型评估报告.csv` | ML模型评估报告 | CSV |
| `analysis_output/model_cache/trained_models.joblib` | 训练好的模型 | joblib |
| `analysis_output/model_cache/eval_report.csv` | 评估报告缓存 | CSV |
| `analysis_output/model_cache/shap_values.pkl` | SHAP值缓存 | pkl |
| `analysis_output/model_cache/data_hash.txt` | 数据指纹 | txt |

### 定量数据字段说明

| 字段名 | 说明 | 单位 |
|--------|------|------|
| 放大倍数 | 图像放大倍数 | x |
| 熔覆层组织面积占比(%) | 熔覆层组织面积百分比 | % |
| 析出相/碳化物面积占比(%) | 析出相/碳化物面积百分比 | % |
| 气孔孔隙率(%) | 气孔面积百分比 | % |
| 微裂纹面积占比(%) | 裂纹面积百分比 | % |
| 熔覆层平均晶粒尺寸(μm) | 熔覆层晶粒平均尺寸 | μm |
| 基体稀释率(%) | 基体稀释程度 | % |
| 激光功率 | 激光功率 | W |
| 图像名称 | 原始图像文件名 | - |

---

## 常见问题

### Q1: 图像分割失败怎么办？

**原因**: 图像格式不支持或损坏

**解决**:
- 确保图像为TIFF格式（.tiff或.tif）
- 检查图像是否损坏
- 支持的图像模式：I;16, I, L, RGB, RGBA, P

### Q2: ML模型训练很慢？

**原因**: Bayesian优化需要多次训练

**解决**:
- 减少trials数量（修改 `n_trials=20`）
- 使用缓存（首次训练后自动缓存）
- 数据量小时可关闭Bayesian优化

### Q3: 如何添加新的图表？

**方法**:
1. 在 `streamlit_app.py` 中定义绘图函数
2. 使用 `@register_plot` 装饰器注册
3. 在图表选择下拉框中自动出现

**示例**:
```python
@register_plot("新图表名称", "new_chart_key")
def plot_new_chart(data):
    fig, ax = plt.subplots()
    # 你的绘图代码
    return fig
```

### Q4: 模型缓存失效怎么办？

**原因**: 数据文件变更或缓存文件损坏

**解决**:
- 删除 `analysis_output/model_cache/` 文件夹
- 重新训练模型

### Q5: 如何停止正在运行的训练？

**方法**:
- 点击侧边栏 "⏹ 停止训练" 按钮
- 或按 Ctrl+C 关闭终端

### Q6: Web界面无法访问？

**原因**: 端口被占用或防火墙阻止

**解决**:
- 更换端口：`streamlit run streamlit_app.py --server.port 8502`
- 检查防火墙设置
- 关闭占用端口的程序

### Q7: 数据变更后需要重新训练吗？

**回答**: 是的，系统会自动检测数据变更并重新训练

**原理**: 使用MD5哈希检测数据文件是否变更

### Q8: 如何导出图表？

**方法**:
- 在图表下方点击 "下载图表" 按钮
- 选择PNG或CSV格式
- 保存到本地

---

## 更新日志

### v1.0 (2026-06-19)

**新增功能**:
- ✅ 图像分割与定量分析
- ✅ 机器学习回归模型（RFR/XGBoost/GBDT/KNN）
- ✅ Bayesian超参数优化（Optuna TPE）
- ✅ SHAP可解释性分析
- ✅ BFGS多目标优化
- ✅ Streamlit Web界面
- ✅ 模型缓存机制
- ✅ 停止训练功能
- ✅ 模块化绘图系统
- ✅ 一键启动脚本

**技术特性**:
- 支持多种图像格式（TIFF, PNG, JPEG）
- 自动数据哈希检测
- 智能缓存管理
- 实时进度显示
- 可中断训练

**输出成果**:
- 8种可视化图表
- Word实验报告
- Excel数据表
- 模型评估报告

---

## 技术支持

**GitHub仓库**: https://github.com/luh-Byte/ml-laser-jg1-q355

**问题反馈**: 请在GitHub Issues中提交问题

**文档更新**: 每次代码更新时同步更新此指南

---

## 许可证

MIT License - 详见 [LICENSE](LICENSE) 文件

---

**祝您使用愉快！** 🚀